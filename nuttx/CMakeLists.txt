cmake_minimum_required(VERSION 3.13)
set(CMAKE_SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/nuttx")
set(ZENOHPICO_ENABLE OFF CACHE BOOL "Enable Zenoh-Pico" FORCE)
if(CONFIG_ZENOH_PICO_NUTTX)
  set(ZENOHPICO_ENABLE ON CACHE BOOL "Enable Zenoh-Pico" FORCE)
  set(CMAKE_SYSTEM_NAME "Generic" CACHE STRING "Set system name to Generic for NuttX" FORCE)
  set(WITH_NUTTX TRUE CACHE BOOL "Build for NuttX RTOS" FORCE)
  message(STATUS "Zenoh-Pico Is Enabled")
  # Build flags from Kconfig
  if(CONFIG_Z_FEATURE_LINK_SERIAL)
    set(Z_FEATURE_LINK_SERIAL 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "Serial Is Enabled")
  endif()
  if(CONFIG_Z_FEATURE_LINK_TCP)
    set(Z_FEATURE_LINK_TCP 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "TCP Is Enabled")
  endif()
  if(CONFIG_Z_FEATURE_LINK_UDP_UNICAST)
    set(Z_FEATURE_LINK_UDP_UNICAST 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "UDP Unicast Is Enabled")
  endif()
  if(CONFIG_Z_FEATURE_LINK_UDP_MULTICAST)
    set(Z_FEATURE_LINK_UDP_MULTICAST 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "UDP Multicast Is Enabled")
  endif()
  if(CONFIG_Z_FEATURE_LINK_BLUETOOTH)
    set(Z_FEATURE_LINK_BLUETOOTH 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "Bluetooth Is Enabled")
  endif()
  if(CONFIG_Z_FEATURE_RAWETH_TRANSPORT)
    set(Z_FEATURE_RAWETH_TRANSPORT 1 CACHE STRING "Toggle Serial links" FORCE)
    message(STATUS "Ethernet Is Enabled")
  endif()
endif()